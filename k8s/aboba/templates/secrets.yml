apiVersion: v1
kind: Secret
metadata:
  name: credentials
  labels:
    app: aboba
    chart: '{{ .Chart.Name }}-{{ .Chart.Version }}'
    release: '{{ .Release.Name }}'
    heritage: '{{ .Release.Service }}'
type: Opaque
data:
  secret: {{ .Values.secretkey | b64enc | quote }}
